{% extends 'base.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}

	<div class="card">
		<div class="card-header d-flex justify-content-between align-items-center">
			<h1>Détails de l'utilisateur {{ user.id }}</h1>
            {{ include('user/_delete_form.html.twig') }}
        </div>
		<div class="card-body">
			<form class="form-horizontal" action="" method="post" enctype="multipart/form-data">
				<div class="form-group row">
					<h4 class="col-md-3 col-form-label">Email</h4>
					<div class="col-md-9">
						<p class="form-control-static">{{ user.email }}</p>
					</div>
				</div>
				<div class="form-group row">
					<h4 class="col-md-3 col-form-label">Roles</h4>
					<div class="col-md-9">
						<p class="form-control-static">{{ user.roles ? user.roles|json_encode : '' }}</p>
					</div>
				</div>
			</form>
		</div>
	</div>


	{# <h1>User</h1>
											
											    <table class="table">
											        <tbody>
											            <tr>
											                <th>Id</th>
											                <td>{{ user.id }}</td>
											            </tr>
											            <tr>
											                <th>Email</th>
											                <td>{{ user.email }}</td>
											            </tr>
											            <tr>
											                <th>Roles</th>
											                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
											            </tr>
											            <tr>
											                <th>Password</th>
											                <td>{{ user.password }}</td>
											            </tr>
											        </tbody>
											    </table>
											
											    <a href="{{ path('user_index') }}">back to list</a>
											
											    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a> #}
	{# <button class="btn btn-primary"> #}
		<a class="btn btn-primary text-light" href="{{ path('user_index') }}">Retour</a>
	{# </button> #}

	{# <button class="btn btn-success"> #}
		<a class="btn btn-success text-light" href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>
	{# </button> #}

{% endblock %}
