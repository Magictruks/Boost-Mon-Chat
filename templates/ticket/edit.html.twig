{% extends 'base.html.twig' %}

{% block title %}Edit Ticket
{% endblock %}

{% block body %}
	<div class="card">
		<div class="card-header">
			<h1>Modifier un {{ controller_name }}</h1>
		</div>
		<div class="card-body">
			{{ include('ticket/_form.html.twig', {'button_label': 'Modification'}) }}
		</div>
	</div>

	<a class="btn btn-primary" href="{{ path('ticket_index') }}">Retour</a>

	{# {{ include('ticket/_delete_form.html.twig') }} #}
{% endblock %}
